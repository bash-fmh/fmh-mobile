enum ViewState {
  idle,
  busy,
  empty,
  error,
}
enum ViewStateErrorType {
  defaultError,
  networkError,
  unauthorizedError,
}
enum AppEnvironment {
  dev,
  uat,
  prod,
}
enum NavButtonType {
  home,
  search,
  profile,
}
enum ButtonStatus {
  active,
  deactivate,
}
enum DeployCountry {
  my,
  id,
  th,
}
enum DeployApplicationType {
  enterprise,
  legacy,
  v2,
}
enum ApplicationType {
  enterprise,
  legacy,
  v2,
}
enum EnterpriseType {
  kfc,
  ph,
}
