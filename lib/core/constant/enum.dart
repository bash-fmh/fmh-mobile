enum ViewState {
  idle,
  busy,
  empty,
  error,
}
enum ViewStateErrorType {
  defaultError,
  networkError,
  unauthorizedError,
}
enum AppEnvironment {
  dev,
  uat,
  prod,
}
enum NavButtonType {
  dashboard,
  notification,
  apps,
}
enum ButtonStatus {
  active,
  deactivate,
}
enum DeployCountry {
  my,
  id,
  th,
}
enum ApplicationType {
  enterprise,
  legacy,
  v2,
}
enum EnterpriseType {
  kfc,
  ph,
}
enum MTDType {
  variance,
  discard,
}
enum QtyVariance {
  cost,
  qty,
}
